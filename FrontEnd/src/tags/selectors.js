/**
 * Created by Orion on 3/18/2018.
 */

import { createSelector } from 'reselect'
import * as _ from 'lodash'

const byId = state => state.messages.byId;

export const tagsByMessage = createSelector(

);